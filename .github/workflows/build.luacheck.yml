name: build-luacheck
on:
  push:
    paths:
      - .github/workflows/build.luacheck.yml
      - images/luacheck/image_data/**/*
      - images/luacheck/test_configs/**/*
      - images/luacheck/BUILD
      - WORKSPACE
      - images/base/*
      - images/base/**/*
      - tests/*.yaml

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Build
        run: |
          bazel build //images/luacheck:image

      - name: Test
        run: |
          bazel test //images/luacheck:test

      - name: Determine tag for image
        id: image
        uses: ./.github/actions/image-tag
        with:
          image: luacheck
          repository: $GITHUB_REPOSITORY

      - name: Tag Image
        run: |
          bazel run //images/luacheck:image
          docker tag bazel/images/luacheck:image ${{ steps.image.outputs.fullname }}

      - name: Push to GitHub Packages
        if: github.ref == 'refs/heads/master'
        run: |
          echo ${{ secrets.GHCR_PAT  }} | docker login ghcr.io --username ${{ github.actor }} --password-stdin
          docker push ${{ steps.image.outputs.fullname}}
