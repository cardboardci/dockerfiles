name: svgtools-hub
on:
  push:
    branches:
      - master
    paths:
    - definitions/svgtools/README.md
    - .github/workflows/svgtools.hub.yml

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Docker Hub Description
        uses: peter-evans/dockerhub-description@v2.1.0
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
          DOCKERHUB_REPOSITORY: cardboardci/svgtools
          README_FILEPATH: definitions/svgtools/README.md