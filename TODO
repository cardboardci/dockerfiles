PATH=/home/circleci/bin:/home/circleci/.local/bin:$PATH

ln -sf /usr/share/zoneinfo/Etc/UTC /etc/localtime

autoconf
build-essential
ca-certificates
# already installed but hear for consistency
curl
git
gnupg
gzip
jq
# for ssh-enabled builds
net-tools
netcat
parallel
# compiling tool
pkg-config
software-properties-common
# already installed but hear for consistency
tzdata
unzip
# for ssh-enabled builds
wget

tar
zip

apt-transport-https \
ca-certificates \
curl \
gnupg-agent \
software-properties-common && \